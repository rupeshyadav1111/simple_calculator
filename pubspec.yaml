name: flutter_application_1
description: "A simple calculator app with network check and new UI."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # UI icons and math operations
  cupertino_icons: ^1.0.8
  math_expressions: ^3.1.0

  # ✅ Added network and settings dependencies
  connectivity_plus: ^6.1.5
  app_settings_plus: ^5.0.0
  fluttertoast: ^8.2.2  # For toast messages

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Add image or other assets here later if needed:
  # assets:
  #   - assets/
